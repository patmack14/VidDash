//import React from "react";
import data from './docdata.js';
//export default firstChart;
let array= [];
//var date = data.map(function(d){return + d.EventDate}); //get undefined map error here
for(let i =0;i<10;i++){
array[i]= data["Event Date"]; //this syntax does not work
console.log(array[i])
}//,

const dataMapper(data)=>{

    const hrsWatched = data.map((row)=>{

    }
}